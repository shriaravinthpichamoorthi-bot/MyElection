# ── Frontend (Vercel) ──
# URL of your Railway backend API
VITE_API_URL=https://your-railway-url.up.railway.app

# Optional: How often the dashboard polls for updates (seconds)
# VITE_POLL_INTERVAL_SECONDS=60

# Optional: Separate Bihar backend URL (only needed if running Bihar live section)
# VITE_BIHAR_API_URL=http://127.0.0.1:8000
